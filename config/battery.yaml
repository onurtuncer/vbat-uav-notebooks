# Battery parameters for the V-BAT tail-sitter sizing study.
#
# specific_energy : pack-level specific energy [Wh/kg]
#   140 Wh/kg is a conservative LiPo pack-level estimate.
#   Cell-level is typically ~200 Wh/kg; the pack overhead (casing,
#   BMS, wiring) reduces this to ~140 Wh/kg at the system level.
#   High-voltage LiHV packs can reach 155–165 Wh/kg.
#
# usable_fraction : usable depth-of-discharge [-]
#   0.85 = 85% DoD. Keeping 15% reserve protects cycle life
#   and avoids cell damage at the low end.
#
# eta_bat : battery discharge efficiency [-]
#   0.916 = mission-averaged I^2*R efficiency at the NOMINAL pack
#   resistance (90 mOhm, config/thermal.yaml R_pack_mohm) on the 6S
#   bus, iterated to the closure fixed point: the heavier battery
#   raises hover current, which lowers the efficiency again (hover legs
#   ~0.86 at ~33 A, cruise ~0.98 at ~4.6 A, energy-weighted over the
#   160 s vertical + 900 s cruise mission). Replaces the former 0.97
#   constant, which implied an unrealistic ~23 mOhm pack (ADR-0014
#   amendment, 2026-07-18); the honest accounting costs ~+215 g MTOW
#   through the closure. Re-derive from the MEASURED pack DCIR at
#   procurement (a 60-70 mOhm build claws most of it back); keep in
#   sync with the R_pack_mohm nominal case.
#
# c_rate_max : maximum continuous discharge C-rate [1/h]
#   25C continuous is a routine rating for hobby LiPo packs.
#   The sizing loop reports the peak mission C-rate for comparison
#   against this limit.

specific_energy: 140.0
usable_fraction: 0.85
eta_bat: 0.916
c_rate_max: 25.0
